<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php
    
    /* Hi ha diferents tipus d'operadors, depenent de lu que vulguem fer:
        1- Operadors aritmètics:
        + Suma
        - Resta
        * Multiplicació
        / Divisió
        % Mòdul o residu de la divisió

        2- Operadors d'assignació:
        $a = $b; Assigna $a pren el valor de $b
        $a += $b; Suma i assigna (És el mateix que $a = $a + $b)
        $a -= $b; Resta i assigna (És el mateix que $a = $a - $b)
        $a *= $b; Multiplica i assigna (És el mateix que $a = $a * $b)
        $a /= $b; Divideix i assigna (És el mateix que $a = $a / $b)
        $a %= $b; Calcula el mòdul i assigna (És el mateix que $a = $a % $b)
        $cadena .= "Joan"; Concatena i assigna  (Concatena "Joan" al final de $cadena)

        3- Operadors de comparació:
        == Igual a
        === Igual a (només si són del mateix tipus)
        != Diferent a
        !== Diferent a (si són de tipus diferents)
        < Menor que
        > Major que
        <> 
        <= Menor o igual que
        >= Major o igual que

        4- Operadors lógics:
        && o 'and' (minuscules): Retorna true si $a i $b són certes
        || o 'OR' (minuscules): Retorna true si alguna de les variables és cert
        XOR (minuscules): Retorna true si $a o $b són certes, pero no les dues alhora
        !: Retorna true si $a no es cert
        
        5- Operadors d'increment:
        ++$a : Incrementa $a en 1 i retorna $a
        $a++ : Retrona $a i llavors incrementa en 1
        --$a : Resta $a en 1 i retorna $a
        $a-- : Retorna $a i llavors resta 1

        6- Operadors de Strings:
        .(punt) : Concatena dues cadenes de text
        .= : Concatena i asigna el resultat a la variable de la esquerra.

        7- Operadors d'Arrays
        + : Uneix dos arrays (mantenint les claus úniques del primer array)
        == : Comproba si dos arrays tenen els mateixos clau-valor
        === : Comproba si els dos arrays son iguals en tipus i ordre
        != o <> : Comproba si els dos arrays no son iguals
        !== : Comproba si dos arrays no son identics.

    */
    ?>
</body>
</html>